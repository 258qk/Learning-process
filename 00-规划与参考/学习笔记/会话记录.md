# 学习会话记录

---

## 第 1 次学习 — 2026-06-07（周日）

### 对话大纲
1. 用户确认学习状态：未正式开始，有 Python/C 基础语法 + 51/STM32 嵌入式经验
2. 确认学习目标：先学三行业（无人机/通信/导航）通用交集知识，再选方向
3. 讨论 NumPy 的作用：PC 端做算法原型验证，验证通后翻译成 C 落地到嵌入式
4. Day 01：环境搭建 + 初始项目结构 + 第一段 NumPy 代码
5. 用户反馈"太快了"——代码太多、术语太多、一次塞了 8 个概念
6. 用户反馈"毫无成就感"——需要有阶段性项目目标

### 重要决策 / 共识
- **教学方式**：先讲应用场景 → 给 5 行代码跑起来 → 逐行解释 → 练习题 → 自检清单
- **每节课限制 3-4 个知识点**，以"能独立写出"为标准
- **禁止"先把基础学完再动手"**——18 个月时间有限，必须边做边学
- **区分"看懂"和"能独立写"**——看懂不等于学会
- **设定了三级项目里程碑**：🥉传感器数据模拟器 → 🥈温度预测模型 → 🥇图像分类器
- **更新了 Skill 教学铁律**：三条铁律（应用场景→跑代码→解释原理；限制知识点；禁止术语）
- **新增了"每天标准流程"**：目标→跑代码→解释+练习→自检→时间检查
- **新增了内容量控制规则**：1h=2-3知识点，2h=3-4知识点，3h=4-5知识点

### 用户新偏好 / 发现
- 用户 Python 生疏但 C 功底好，C 类比教学效果最好
- 用户讨厌"纯理论先学完再动手"的方式
- 用户需要明确的成就感节点（项目里程碑）
- 用户会主动纠正 AI 的节奏问题——需要尊重这种反馈
- 用户写代码喜欢用 C 思维（for i in list 踩坑），这是正常的过渡期

### 当前进度状态
- 阶段：阶段一（M1-M3）第 1 周
- 已完成：Day 01（ndarray 创建、运算、vs Python 列表）
- 已踩坑：Python for 循环中 i 是值不是下标
- 进度百分比：约 4%
- 下一个里程碑：🥉青铜项目「传感器数据模拟器」（预计 Day 04-05）

### 下次计划
Day 02：linspace、arange、切片、Python vs NumPy 对比练习

---

## 第 2 次学习 — 2026-06-08（周一）

### 对话大纲
1. Day 02 教学：linspace vs arange 区别、切片语法、Python 列表 vs NumPy 对比
2. 5 道练习题：生成采样点、批量运算、切片、数学运算、思考题
3. 用户反馈"内容太少"——20 分钟就做完了，计划每天 3 小时只给了 20 分钟的量
4. 立即追加 Day 03：reshape、广播（broadcasting）、axis 方向运算
5. Day 03 3 道练习题全部完成
6. 用户问 MCP 服务：介绍 Python REPL MCP、GitHub MCP，建议暂不配置
7. 用户确认学习目标：不是纯软件工程师，是懂 AI 的嵌入式/系统工程师
8. 用户要求创建"学习会话记录"skill，防止上下文丢失

### 重要决策 / 共识
- **我的失误**：Day 02 内容量严重不足（20min vs 计划 3h），是教练规划问题，不是用户的问题
- **改正措施**：Skill 新增"时间检查"步骤——每节课结束主动问实际花了多少分钟，< 计划 50% 就追加内容
- **Skill 新增"内容量控制规则"表**：按投入时间匹配知识点数量和练习题数量
- **Skill 新增 GitHub 强制规则**：每天课前确认 git 初始化，课后提醒 commit
- **用户目标再确认**：最终交付是"Python 训练 → C++ 部署 → 端侧推理"全链路，不是纯 AI 调参

### 用户新偏好 / 发现
- 用户会主动要求改进 skill 文件来规范后续教学
- 用户关心"学了有没有用"——每个知识点需要先讲应用场景
- 用户需要教练主动管理学习节奏（内容量、时间检查），不能被动等用户反馈
- 用户反感"做完了不知道该干什么"的状态
- 用户担心纯软件方向容易被取代，需要反复确认路线不走偏

### 当前进度状态
- 阶段：阶段一（M1-M3）第 1 周
- 已完成：Day 01 / Day 02 / Day 03（NumPy 基础课全部通关）
- 累积学习时间：约 3h
- 已踩坑归档：6 个
- 进度百分比：约 12%
- 下一个里程碑：🥉青铜项目「传感器数据模拟器」（下一次学习开始）

### 下次计划
🥉青铜项目：传感器数据模拟器——用 NumPy 生成模拟 ADC 采样数据，计算统计量，导出 CSV。第一个完整的 GitHub 作品。

---

## 第 3 次学习 — 2026-06-09（周二）

### 对话大纲
1. 中午：Git 仓库初始化 + 首次 commit（13 个文件）
2. 中午：青铜项目起步——sensor_sim.py 基础代码跑通（3 通道 + 统计 + CSV + 可视化）
3. 中午：逐行讲解 random.seed、randn、正弦波模拟传感器、matplotlib 7 函数、.format vs f-string、np.abs
4. 晚上 19:57 继续：3 道扩展题（GPS 通道、滑动窗口对比、异常点检测）
5. 用户遇到多个问题：subplot 行数不够、column_stack 长度不匹配、delimiter 拼写错误
6. 用户问 os.path.join 含义、np.random 为什么是模块、argmax 用法
7. 用户正确理解了 GPS 递增逻辑（`i × 步长`），纠正了 C 思维误区
8. 代码审查 3 轮，逐步修复所有问题
9. 用户自行写出自检代码（独立完成），证明真正掌握
10. 用户提出核心问题：知道思路但记不住函数名——类比 STM32 标准库确认这是正常状态
11. commit + 更新全部笔记（进度/困难/会话）

### 重要决策 / 共识
- **Git 仓库已初始化**：今后每次学习 commit 成为习惯
- **项目代码独立目录**：`project-sensor-sim/` 与练习题 `python-numpy/` 分开
- **函数名记不住 ≠ 没掌握**：STM32 的 `HAL_UART_Transmit` 也不是背的。区分"不知道做什么"（严重）和"忘了函数名"（正常）
- **青铜项目通过标准**：6 项自检中 5 项独立写出，仅函数名偶需查旧代码

### 用户新偏好 / 发现
- 用户会主动在代码末尾写验证/自检代码（很好的习惯）
- 用户对"递增"的 C 思维（每步加固定值）需要刻意纠正为 NumPy 思维（arange × 步长）
- 用户对信号仿真完全陌生但学习速度快——C 功底帮助很大
- 用户关心拼写错误和函数名记忆问题，需要明确区分"真不会"和"暂时记不住"

### 当前进度状态
- 阶段：阶段一（M1-M3）第 1 周
- 已完成：Day 01 / Day 02 / Day 03 + 🥉青铜项目
- 累积学习时间：约 5h
- 已踩坑归档：11 个
- GitHub commits：2 次
- 进度百分比：约 20%
- 下一个里程碑：🥈白银项目「温度预测模型」（NumPy 进阶后进入 PyTorch）

### 下次计划
用户确认：直接进 PyTorch，线性代数在 PyTorch 学习中遇到时现补。

---

## 第 4 次学习 — 2026-06-10（周三）

### 对话大纲
1. 用户确认进 PyTorch
2. PyTorch 未安装 → 用户用清华镜像源自装 `pip install torch -i https://pypi.tuna.tsinghua.edu.cn/simple`
3. PyTorch Day 01 教学：Tensor 创建/运算/切片/与 NumPy 互转
4. 用户踩坑：`numpy.tensor()` 不存在（正确是 `tensor.numpy()`）
5. 用户提出切片规则混淆 → 逐行讲解二维切片 `[行,列]`，厘清"起始位=N-1、停止位=N"规则
6. 用户独立写出自检代码，自检清单全部通过
7. 用户新建"嵌入式线-RTOS-ROS"双轨学习规划，我指出时间不可行（4.5h/天），建议精简，用户决定先保留不调整
8. 用户确认：嵌入式线自己听网课推进，AI 主线保持现有节奏

### 重要决策 / 共识
- **PyTorch 安装**：清华镜像源加速，CPU 版 2.12.0
- **双轨策略**：AI 主线（每天 ~2h）+ 嵌入式辅线（用户自行推进，不占用 AI 主线时间）
- **RTOS 嵌入时机**：阶段二（M4-M6）开始，每天 30min，不挤占 AI 主线
- **ROS 嵌入时机**：阶段四（M10-M13）集中 3 天学
- **切片规则统一记法**：起始位=N-1，停止位=N（和 C 的 `for(i=0;i<n;i++)` 同源）

### 用户新偏好 / 发现
- 用户会主动建设学习资料（"嵌入式线"两个 md 文件质量高）
- 用户对切片规则有 C 程序员特有的困惑——"第 1 个"和"下标 0"的对应关系需要刻意练习
- 用户倾向于自己安装/配置环境（不用我代劳）

### 当前进度状态
- 阶段：阶段一（M1-M3）第 1 周
- 已完成：Day 01/02/03 + 🥉青铜项目 + PyTorch Day 01
- 累积学习时间：约 6h
- 已踩坑归档：14 个
- GitHub commits：3 次
- 进度百分比：约 25%
- 下一个里程碑：PyTorch Day 02（自动求导 autograd）→ 🥈白银项目

### 下次计划
PyTorch Day 05：线性回归实战（用 autograd 训练第一个模型，3h 完整课）。

---

## 第 5 次学习 — 2026-06-10（周三·下半场）

### 对话大纲
1. 用户要求：每天严格 3h，内容不够就追加
2. PyTorch Day 04 autograd 开讲：3 个知识点（requires_grad / backward / .grad）
3. 用户深入追问：grad 为什么是 6 不是 7（瞬时变化率 vs 实际变化）
4. 用户追问 backward() 的作用（录音 vs 倒放）
5. 用户追问梯度 vs 导数 vs 斜率的名称关系
6. 用户纠正命名规则：普通学习 `day0X_主题.py`，项目 `dXX_项目名.py`
7. Skill 新增：文件命名规则 + 代码注释规则（前置依赖用 ⚠️ 标注）
8. 3 道自写题（用户独立写代码）：单变量/多变量/参数更新循环
9. 用户遇到问题：while 条件报错（Tensor vs int）、no_grad 放错位置、画图为直线
10. 用户自主用 matplotlib 画梯度下降曲线（学了即用，主动拓展）
11. 用户深入理解：梯度自动刹车原理、loss 函数 U 形底部、目标值任意都能逼近
12. 学习流程规范化：我出题只给框架，用户独立填代码

### 重要决策 / 共识
- **内容纪律**：每节课必须撑满 3h，不够就追加。由我负责监控，不是用户来催
- **命名规则统一**：日常学习 `day0X_主题.py`，项目 `dXX_项目名.py`，编号跨模块全局递增
- **代码注释强制规则**：有前置依赖的代码必须用 ⚠️ 标注（如 backward 必须在 .grad 之前）
- **自写题模式**：我只给公式和目标，用户独立写代码，不再代写
- **while 循环逼近**：用户自主要求用 while 替代固定循环，直到目标误差 < 阈值

### 用户新偏好 / 发现
- 用户会主动用上一课学到的工具（matplotlib）做可视化验证——学习迁移能力强
- 用户对"为什么"追问很深入（grad=6 不是 7、为什么设 0.01 学习率）
- 用户对自己写的代码会主动调试（while 报错、画图为直线），不怕犯错
- 用户能独立思考"while 逼近目标值"和"break 停止循环"的方案

### 当前进度状态
- 阶段：阶段一（M1-M3）第 1 周
- 已完成：Day 01/02/03 + 🥉青铜项目 + Day 04（Tensor + autograd 全链路）
- 累积学习时间：约 8.5h
- 已踩坑归档：18 个（今天新增 6 个 autograd 相关）
- GitHub commits：4 次
- 进度百分比：约 30%
- 下一个里程碑：PyTorch Day 05（线性回归实战）→ 🥈白银项目

### 下次计划
PyTorch Day 06：DataLoader + 数据集加载。

---

## 第 6 次学习 — 2026-06-11（周四）

### 对话大纲
1. 开场 git 残留文件清理——中午文路径 `git rm` 误删阶段一目录，`checkout` 恢复
2. PyTorch Day 05 线性回归开讲：4 个知识点（双参数训练 / MSE / w vs b 梯度 / 拟合可视化）
3. 用户深入追问：b 的梯度含义（全局偏移修正，不含 x 所以收敛慢）、backward 链式法则细节
4. 用户要求推演过程：从 w=0, b=0 开始逐步打印梯度→分析 w 快 b 慢的原因
5. 用户主动要求实现"拟合线演变动画"：存 w/b 历史值，渐变画线
6. enumerate/zip 语法教学——用户嫌 zip 绕，改回 `for i in range()`（C 风格）
7. scatter / legend / alpha / savefig 等新 matplotlib 函数逐个解释
8. 自检：用户独立写出二次函数拟合 `y=wx²+b`，for→while 循环，渐变线可视化
9. 笔记 + commit + push（用户指出漏了会话记录和困难总结）

### 重要决策 / 共识
- **中文路径禁止 git rm**：用 PowerShell Rename-Item 替代，防止误删
- **Python 语法不强推高级写法**：用户更习惯 C 风格 `for i in range()`，优先用熟悉的
- **拟合线演变可视化**：用户主动提出存 w/b 数组 → 渐变画线，学习主动性高

### 用户新偏好 / 发现
- 用户对"可视化验证"有天然兴趣——看到拟合线从浅到深收敛比看数字更有感觉
- 用户遇到新语法（enumerate/zip）会追问到底，不理解就不接受——这是好的
- 用户能独立完成"把线性模型改成二次模型"的自检——说明已掌握核心机制

### 当前进度状态
- 阶段：阶段一（M1-M3）第 1 周
- 已完成：Day 01-05 + 🥉青铜项目
- 累积学习时间：约 11h
- 已踩坑归档：21 个（今天新增 3 个）
- GitHub commits：6 次
- 进度百分比：约 35%
- 下一个里程碑：PyTorch Day 06（DataLoader）

### 下次计划
PyTorch Day 06：DataLoader + 数据集加载——批量数据喂给模型。

---

## 第 7 次学习 — 2026-06-12（周五）

### 对话大纲
1. DataLoader 教学：TensorDataset / DataLoader / batch_x,batch_y 取 batch
2. 用户逐步追问：dataset 为什么不打印数据 → 容器；loader 是什么 → 迭代器不是数组
3. 元组拆包解释：`for batch_x, batch_y in loader` 怎么自动赋值
4. unsqueeze(1) 加维度原因 / shape 属性 vs shape() 方法踩坑
5. 广播 bug：`(100,1) + (100,)` 炸成 `(100,100)` — 维度必须对齐
6. 自检：用 DataLoader 重写二次函数训练循环
7. 踩坑 nan：x=0~100 + 二次函数导致梯度爆炸 → 调小 x 范围 + w 学习率降到 0.0001
8. 用户成功跑出 w=3.01, b=3.48

### 重要决策 / 共识
- **我节奏太快被用户纠正**：用户说"别这么快""我没有说下一步就不要下一步"，以后严格等用户说"下一步"再前进
- **二次函数学习率不对称**：w 的梯度含 x²（放大百倍），学习率要极小（0.0001）；b 不含 x²，学习率正常（0.005）

### 用户新偏好 / 发现
- 用户对"抽象概念"需要反复问到底——不理解不会放过（DataLoader 问了三四轮）
- 用户能独立把"线性回归"改成"二次函数回归"——说明核心机制（backward/更新/循环）已内化
- 用户会主动主动概括两种写法的区别，不是被动接受

### 当前进度状态
- 阶段：阶段一（M1-M3）第 2 周
- 已完成：Day 01-06 + 🥉青铜项目
- 累积学习时间：约 12.3h
- 已踩坑归档：24 个（今天新增 3 个）
- GitHub commits：8 次
- 进度百分比：约 40%
- 下一个里程碑：PyTorch Day 07（nn.Module）

### 下次计划
PyTorch Day 07：nn.Module + nn.Linear——用 PyTorch 内置层替代手写 w,b。

---

## 第 8 次学习 — 2026-06-15（周一）

### 对话大纲
1. 全面复习 Day 01-06 全部 API（45 个函数口答测验）
2. 分组测验：NumPy 创建 / NumPy 运算 / Tensor / autograd / 训练循环
3. 主要缺口：NumPy 运算类最弱（sum/std/argmax/reshape/column_stack/savetxt）
4. 关键漏洞确认：zero_() 不写 = 梯度累加 = 方向错误
5. 机器学习宏观问题：线性回归属于机器学习范畴；传统 ML 暂时不学

### 重要决策 / 共识
- 传统 ML（SVM/决策树/KNN）不列入学习路径，三行业核心是深度学习
- 面试前花 3 天扫 sklearn API 即可

### 当前进度状态
- 阶段：阶段一（M1-M3）第 2 周
- 已完成：Day 01-06 + 复习
- 累积学习时间：约 12.8h
- 进度百分比：约 42%
- 下一个里程碑：PyTorch Day 07（nn.Module）

### 下次计划
PyTorch Day 07：nn.Module + nn.Linear + Optimizer——用内置 API 替代手写参数。

---

## 第 9 次学习 — 2026-06-18（周四）

### 对话大纲
1. Day 07 开讲：nn.Linear(1,1) 替代手写 w 和 b
2. 用户追问：Linear 参数含义（特征数 vs 样本数）
3. model.parameters() + list() 列出可训练参数
4. torch.optim.SGD + optimizer.step() + zero_grad() 替代手写 4 行
5. 用户发现：optimizer.step() 没包 no_grad()，纠正——optimizer 内部绕过 autograd
6. 用户自写训练循环，踩坑 y_pred.item() 多值报错
7. detach() + clone() 讲解：撕标签 + 深拷贝
8. 用户自选用 for epoch + 收集 w/b 历史值
9. 输出 w=3.12, b=1.07（目标 3.00/2.00）
10. Skill 新增"教学节奏铁律"，永不过期

### 重要决策 / 共识
- **训练模板定版**：pred→loss→backward→step→zero_grad，往后所有模型共用
- Linear 参数是特征数不是样本数——这是最高频踩坑概念
- optimizer.step() 不需要 no_grad()（内部绕过 autograd）

### 用户新偏好 / 发现
- 用户能独立把"新手困惑"组织成系统性追问（原理/参数变化/返回值/为什么用）
- 用户会主动用 `Ctrl+K+C/U` 注释快捷键
- 用户对自写代码有意识加 w/b 历史记录和可视化

### 当前进度状态
- 阶段：阶段一（M1-M3）第 2 周
- 已完成：Day 01-07 + 🥉青铜项目
- 累积学习时间：约 13.8h
- 已踩坑归档：27 个（今天新增 3 个）
- GitHub commits：9 次
- 进度百分比：约 48%
- 下一个里程碑：Day 08（训练/验证/测试集划分 + 过拟合）

### 下次计划
PyTorch Day 08：训练集/验证集/测试集——防止过拟合的正确姿势。
